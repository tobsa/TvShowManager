<Window x:Class="TvShowManagerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:uc="clr-namespace:TvShowManagerWPF.TvShowTracker"
        xmlns:local="clr-namespace:TvShowManagerWPF"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        Title="Tv Show Manager" 
        Width="1600"
        Height="900"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
        
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label HorizontalAlignment="Center">Tv Show Manager</Label>
            
            <Menu Margin="0 0 0 25">
                <MenuItem Header="File" />
                <MenuItem Header="Tv Show Tracker">
                    <MenuItem Header="Web Scrapers" />
                    <MenuItem Header="Link Management" />
                </MenuItem>
            </Menu>
            
        </StackPanel>


        <TabControl Grid.Row="1">
            <TabItem Header="Tv Show Tracker">
                <uc:TvShowTracker DataContext="{Binding TvShowTrackerViewModel}" />
            </TabItem>
            <TabItem Header="Tv Show Viewer"></TabItem>
            <TabItem Header="Movie Viewer"></TabItem>
            <TabItem Header="Movie Transfer"></TabItem>
            <TabItem Header="Subtitle Matcher"></TabItem>
            <TabItem Header="Subtitle Renamer"></TabItem>
            <TabItem Header="IDX Styler"></TabItem>
        </TabControl>
    </Grid>
   
</Window>
